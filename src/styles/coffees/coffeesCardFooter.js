import styled from "styled-components";

const CoffeesCardFooter = styled.div`
  align-items: center;
  display: flex;
  justify-content: space-between;
  width: 100%;

  > div {
    align-items: center;
      display: flex;
      gap: 3px;
  }

  p {
    line-height: 0.75rem;
  }
`

export default CoffeesCardFooter